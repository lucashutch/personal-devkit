---
description: ammend the last commitand force push
agent: build
---
I want you to use your `bash` tool to perform the following pipeline in order:

1. Run the project's formatting and linting commands.
2. If everything passes, amend the last commit with any new changes. Read the diff to update the commit message with a clear, succinct commit message.
3. Push the amended commit to the remote repository with force-with-lease.
